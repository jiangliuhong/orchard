# Agent 工作规则

## 落库确认

涉及数据库、持久化存储或其他会改变用户数据的操作时：

1. 先说明将要写入的数据、目标位置、影响范围及执行方式。
2. 仅完成查询、分析、校验和生成变更方案。
3. 在真正执行写入、更新、删除、迁移或提交前，必须明确询问用户确认。
4. 未获得明确确认时，不得执行任何落库操作。
5. 用户确认仅对当前明确描述的操作有效；后续不同操作需要再次确认。
6. 若操作可能造成不可逆数据变更，必须同时说明风险和回滚方式。

## 代码变更

- 修改前先检查相关代码、测试和项目约定。
- 优先进行最小范围修改，避免无关重构。
- 修改后运行适用的类型检查、构建和测试。
- 不得擅自提交 Git commit、推送远程仓库或发布版本。
